<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dani.firedetection">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Fire Detection"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name="com.dani.firedetection.MapaAdminActivity"
            android:label="@string/title_activity_mapa_admin" />
        <activity
            android:name="com.dani.firedetection.IncendiosDetectadosActivity"
            android:label="Ultimo incendio detectado"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity
            android:name="com.dani.firedetection.InfoDatosIncendioActivity"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity
            android:name="com.dani.firedetection.InfoMapaActivity"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity
            android:name="com.dani.firedetection.MiCuentaActivity"
            android:label="Datos"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity
            android:name="com.dani.firedetection.ModfiDatosActivity"
            android:label="Modificar datos"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.dani.firedetection.EnviadoAvisoActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.dani.firedetection.ValidacionActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.dani.firedetection.MapaActivity"
            android:screenOrientation="portrait" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name="com.dani.firedetection.ResetContraActivity"
            android:label="Restablecer contraseña"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.dani.firedetection.LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.dani.firedetection.DatosIncendioActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.dani.firedetection.RegistrarActivity"
            android:label="Registrar usuario"
            android:screenOrientation="portrait" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>